Console
=======

A console that can be used to experiment JScript code, in a read-eval-print loop fashion.

Usage
-----

Open a Windows command prompt, change to the directory of the console script and execute the following:

    cscript console.js